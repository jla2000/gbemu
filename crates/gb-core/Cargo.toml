[package]
name = "gb-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "DMG Game Boy emulation core (no I/O dependencies)"

[dependencies]
ringbuf = "0.5.0"
